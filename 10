#include <iostream>
#include <math.h>


using namespace std;
int main() {
    int x;
    int m, n;
    cin >> m;
    cin >> n;
    for (int i = m; i <= n; ++i) {
        x = i;
        double y = (x+3)/(sqrt(x-5));
        if(isnan(y)==1){
            cout << "***" << "\n";
        }else if (isinf(y)==1) {
            cout << "***" << "\n";
        }else
        cout << y << "\n";
    }
}
